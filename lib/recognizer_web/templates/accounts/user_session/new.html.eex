<div class="box">
  <h2 class="title is-2 mb-5 has-text-centered-mobile">Log In</h2>

  <%= form_for @conn, Routes.user_session_path(@conn, :create), [as: :user], fn f -> %>
    <div class="field">
      <%= label f, :email, "Email Address", class: "label" %>

      <div class="control">
        <%= email_input f, :email, class: input_classes(f, :email), required: true %>
      </div>

      <%= error_tag f, :email %>
    </div>

    <div class="field">
      <%= label f, :password, class: "label" %>

      <div class="control">
        <%= password_input f, :password, class: input_classes(f, :password), required: true %>
      </div>

      <%= error_tag f, :password %>
    </div>

    <div class="buttons is-right mt-5">
      <div class="control">
        <%= submit "Log in", class: "button is-secondary" %>
      </div>
    </div>

    <div class="content oauth mb-6">
      <p>Or log in with</p>
      <div class="buttons is-centered">
        <%= link to: Routes.o_auth_path(@conn, :request, "github"), class: "github" do %>
          <i class="fa fa-github"></i>
        <% end %>

        <%= link to: Routes.o_auth_path(@conn, :request, "google"), class: "google" do %>
          <i class="fa fa-google"></i>
        <% end %>
      </div>
    </div>
  <% end %>

  <hr class="has-background-grey-lighter" />

  <p class="content">
    <%= link "Forgot your password?", to: Routes.user_reset_password_path(@conn, :new) %>
  </p>
  <p class="content">
    New to System76? <%= link "Create Account", to: Routes.user_registration_path(@conn, :new) %>
  </p>
</div>
